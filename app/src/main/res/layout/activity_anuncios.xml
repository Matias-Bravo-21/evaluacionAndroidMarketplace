<?xml version="1.0" encoding="utf-8"?>
<androidx.coordinatorlayout.widget.CoordinatorLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#F2F5F7"
    tools:context=".AnunciosActivity">

    <!-- 1. AppBar con Toolbar -->
    <com.google.android.material.appbar.AppBarLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="#1E3A5F"
        android:elevation="4dp">

        <androidx.appcompat.widget.Toolbar
            android:id="@+id/toolbar_anuncios"
            android:layout_width="match_parent"
            android:layout_height="?attr/actionBarSize"
            android:background="#1E3A5F"
            app:title="Gestion de Anuncios MatiasMarketplace"
            app:titleTextColor="#FFFFFF"
            app:navigationIcon="@drawable/ic_arrow_back"
            android:theme="@style/ThemeOverlay.AppCompat.Dark.ActionBar" />
    </com.google.android.material.appbar.AppBarLayout>

    <!-- 2. RecyclerView principal -->
    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/rv_mis_productos"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        app:layout_behavior="@string/appbar_scrolling_view_behavior"
        android:paddingBottom="64dp"
        android:clipToPadding="false"
        android:background="#F2F5F7"
        tools:listitem="@layout/activity_item_producto" />

    <!-- 3. Barra de navegación inferior -->
    <com.google.android.material.bottomnavigation.BottomNavigationView
        android:id="@+id/bottom_navigation_view"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_gravity="bottom"
        android:background="#FFFFFF"
        app:itemIconTint="@color/selected_nav_icon"
        app:itemTextColor="@color/select_nav_text"
        app:itemRippleColor="#E0E7FF"
        app:menu="@menu/bottom_nav_menu"
        app:labelVisibilityMode="labeled"
        app:itemActiveIndicatorStyle="@style/ActiveIndicatorStyle" />
</androidx.coordinatorlayout.widget.CoordinatorLayout>
